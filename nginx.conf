server {
    listen 4800;

    client_header_buffer_size 64k;
    large_client_header_buffers 4 64k;

    location /erp/console/ {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /erp/console/index.html;
    }
}